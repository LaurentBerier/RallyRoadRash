# Local tooling that has no business on the platform. Everything else —
# including dev/ harnesses, tests/ and docs/ — is kept: it documents the
# game and runs fine as static files.
.claude/
.gitignore
.sandscapeignore
# Preserve downloaded texture masters locally; runtime uses optimized WebP.
assets/garage/*.jpg
assets/ground/*-v2.png
assets/foliage/*.png
