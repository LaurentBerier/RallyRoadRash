# Imagegen benchmark prompt

Use case: ui-mockup. Create an AAA racing-game UI art-direction benchmark from the two attached existing Rally Road Rash screenshots. Produce one polished landscape design board with two large matching 16:9 menu mockups stacked vertically: STAGE SELECT then GARAGE. Preserve this game's orange accent, charcoal motorsport panels, off-white italic titles, canyon environments and weathered rally buggy identity. Upgrade hierarchy, restrained premium finish, precise spacing, legible labels, understated hairline framing. Each screen must have identical LOCKED layout anchors: narrow brand/header at top, fixed left information column about 35% width with fixed two-line title box, short description and stats/map, a generous right image or 3D-preview frame about 60% width, bottom single row of equal-sized selectable thumbnails, anchored footer with BACK left and primary action right. No overlapping background vehicles, no free-floating repeated text, no clipped cards, no scrolling entire menu, no clutter. Garage right preview is a clean well-lit studio view of current buggy with no duplicate image behind it. Stage select right frame shows the canyon road; left compact map and race information. Orange selected underline, numbered cards, small contextual uppercase labels. Supporting small strip beneath the two mockups can show matching settings rows, playbook tab, loading progress and pause button components. Text: RALLY ROAD RASH, STAGE SELECT, PROVING GROUNDS, GARAGE, DUNE HOPPER, NEXT, START RACE. This is a visual benchmark to implement in HTML/CSS, not an in-game flattened image. Aim for a credible premium racing franchise menu, restrained sophisticated finish, consistent grid and fixed frames.

References: .sandscape/ui-pass/before-tracks.png and before-garage.png.
Output: docs/ui/benchmark.webp. Original generated PNG preserved in the Codex generated_images directory.
